# Core pip-installable dependencies for ML Shock Time Series Synthesis.
#
# NVIDIA CUDA 13.0 users should first install:
#   python -m pip install -r requirements-cu130.txt
#
# The package itself can then be installed without resolving dependencies again:
#   python -m pip install -r requirements.txt
#   python -m pip install -e . --no-deps

numpy>=1.26
scipy>=1.11
matplotlib>=3.8
pandas>=2.2
h5py>=3.10
xarray>=2024.1.0
tqdm>=4.66
natsort>=8.4
opt_einsum>=3.3
einops>=0.7
networkx>=3.2
typing_extensions>=4.10
torchinfo>=1.8
torchdiffeq>=0.2.4
PyWavelets>=1.6
ptwt>=1.0.1